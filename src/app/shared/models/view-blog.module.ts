export interface viewBlog{
    id?: string,
    thumbnailImage?: any,
    image?: string,
    pdf?: string,
    blogTitle?: string,
    blogCategory?: string,
    blogDescription?: string,
    blogStatus?: boolean,
    paymentStatus?: boolean,
    createdAt?: string
}