export interface Memory{
    id?: string,
    uploadedImage?: string,
    logoImage?: string,
    imageDeadLine?: string,
    imageApprove?: boolean,
    imageStatus?: boolean,
    uploadedBy?: string,
    phoneNumber?: string,
    createdAt?: string
}